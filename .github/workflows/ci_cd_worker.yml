name: Deploy worker

on:
  workflow_call:
    inputs:
      worker:
        type: string
        required: true
        description: Worker name

jobs:
  deploy:
    name: "Echo worker name"
    runs-on: ubuntu-latest
    steps:
      - name: Process value
        run: echo "Processing value ${{ inputs.worker }}"
